dependencies:
  pre:
    - sudo pip install awscli
  cache_directories:
    - vendor/bundler
deployment:
  elasticbeanstalk:
    branch: master
    commands:
      - bin/setup-git.sh
      - bin/setup-aws-credentials.sh
      - bin/setup-eb.sh
      - bin/deploy.sh
